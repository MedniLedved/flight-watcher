# API zdroje (Sky Scrapper, Travelpayouts, FX kurzy, …) volají REST přímo
# přes requests – žádné SDK (amadeus balík odstraněn, nikdy se neimportoval
# a jen zpomaloval/rozbíjel pip install).
requests>=2.31.0
fast-flights>=2.2,<3    # Google Flights scraping (primární zdroj cen)
letsfg>=2026.5          # LetsFG – reálné ceny ze 400+ aerolinií (free)
feedparser>=6.0.10
beautifulsoup4>=4.12.0
pyyaml>=6.0
python-dotenv>=1.0.0
lxml>=4.9.0             # Rychlejší parser pro BeautifulSoup
